import org.openqa.selenium.Webdriver;

public class SearchBox extends anasayfa {

    public SearchBox(WebDriver driver) {
        super (driver);
    }
    public void search(String ceket) {
    }
}
